<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
        integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <title>TinyApp</title>
</head>

<!-- Add fixed header -->
<% include partials/_header %>

  <!--Do no display content if logged out -->
<% if (usrObj !== undefined) { %>
<body style="font-size:20px; padding:70px 0 35px;">
    <table style="width:65%; margin:0 auto; text-align:left;" class="table table-hover">
         <thead><tr><th>Redirect URL</th> <th>Shortened URL</th> <th style="text-align:right;">Action</th></tr></thead>
         <% for (var url in urls)  { %>
            <tr><td><a href='<%= urls[url] %>'><%= urls[url] %></a></td>
            <td><a href='/u/<%= url %>'> http://localhost:8080/u/<%= url %></a></td>
            <td><div style="text-align:right;"><div style="display: inline-block;"><a href="/urls/<%=url%>"><button style="float:right; cursor:pointer;">Update</button></a></div>
            <form style="display: inline-block;" method="POST" action="/urls/<%=url%>/delete"><button style="float:right;" type="submit">Delete</button></form></div></td></tr>
        <% } %>
    </table>
</body>
  <% } %>
</html>
