<!-- views/partials/_header.ejs -->
<!DOCTYPE html>
<%
var showLogin = "table";
var showLogout = "none";

if (usrObj)
{
showLogin = (usrObj["id"] === undefined) ? "table" : "none";
showLogout = (usrObj["id"] !== undefined) ? "table" : "none";
}
console.log("blah", usrObj);
%>

<header style="margin: 0 auto; text-align: center;">

<title>TinyApp URL Shortener</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
        integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <h3><%=(usrObj !== undefined) ? "Hello " + usrObj.email + ", Welcome to " : "";%> TinyApp<h3>
  <% if (usrObj === undefined) {%> <% include _login%> <% } %>

</header>

<!-- Add create url and logout forms and buttons -->
<table style="display: <%=showLogout%>; margin:0 auto 12px auto; text-align:left; width: 280px;" class="table table-zebra">
<tr style="border-bottom: 1px solid #dee2e6;">
<% if (usrObj !== undefined) { %><td><a href="/urls/new"><button style="margin-left: 0.25em;" class="btn btn-success">Create New URL</button></a></td><% } %>
<td><form style="text-align:center;" method="POST" action="/logout">
<button style="margin-left: 0.25em;" class="btn btn-danger" type="submit">Logout</button></form></td></tr>
